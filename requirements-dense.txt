# Dense Retrieval Route runtime dependencies. If these or the versioned local
# assets are absent, the live Agent disables this route and falls back locally.
qdrant-client>=1.9,<2
torch>=2.2
transformers>=4.40
numpy>=1.26

# Development only: fetches the bundled embedding model once. The scoring path
# loads from disk and must never download.
huggingface-hub>=0.23
