# Dense Retrieval Route (Slice 04). The official Agent contract still runs on the
# standard library alone until Slice 05 wires this route into live recommendations.
qdrant-client>=1.9,<2
torch>=2.2
transformers>=4.40
numpy>=1.26

# Development only: fetches the bundled embedding model once. The scoring path
# loads from disk and must never download.
huggingface-hub>=0.23
